import { useState } from 'react'

const App = () => {
  const [value, setValue] = useState(0)

  return (
    <div>
      <button onClick= > reset to zero</button>
    </div>
  )
}

export default App
